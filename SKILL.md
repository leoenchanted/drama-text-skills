---
name: drama-text-skills-v2
description: 短剧解说文案生成器 v2.0 - 从爆款短剧爽文结构角度深度拆解字幕，生成结构化剧情分析和800-1000字爆款解说文案，支持口语化英文翻译。从情绪推进、信息差、打脸节奏等维度解析短剧上瘾机制。
metadata:
  tags: copywriting, short-drama, subtitle, script, translation, v2.0
---

## 触发场景

当用户说「帮我给这个短剧写解说文案」「把字幕生成文案脚本」「drama copywriting」等，或提供字幕 .txt 文件要求生成短视频解说文案时，使用此 Skill。

## 输入要求

用户需要提供：
1. **短剧字幕文件**（.txt 格式）
2. **剧情简介**（可选）— 建议字幕来自中间集数时提供，帮助理解人物关系和前情背景
   - 方式 A（推荐，对话粘贴）：在对话中以 `剧情简介：` 开头，后面直接粘贴简介正文（可多行）
   - 方式 B（文件）：提供 .txt 文件，文件名含"剧情""简介""summary""plot"等关键词
   - 如果未提供，仅靠字幕内容理解剧情
3. **短剧视频文件**（可选）— 当字幕来自画面表现较多、对白较少的剧集时提供
   - 推荐与字幕同目录同名，如 `episode01.mp4` + `episode01.txt`
   - 支持 `.mp4` / `.mov` / `.mkv` 等常见视频格式
   - 字幕必须带时间轴，优先支持 SRT 保存成 TXT 的格式：`00:00:01,000 --> 00:00:03,000`

## 模板文件夹（重要）

模板文件夹位于**本 SKILL.md 文件所在目录下的 `templates/` 子文件夹**。

查找步骤：
1. 本 SKILL.md 位于 `.claude/skills/drama-text-skills/SKILL.md`
2. 模板文件夹就在同目录：`.claude/skills/drama-text-skills/templates/`
3. 先用 `ls .claude/skills/drama-text-skills/templates/` 确认目录存在
4. 如果报「目录不存在」或「无法访问」，说明 CWD 不是项目根目录。此时用 `find . -path "*/drama-text-skills/templates" -type d` 定位实际路径，然后读取
5. 读取其中所有模板文件（.txt / .md）
6. 如果模板文件夹为空，提醒用户先把爆款文案模板放入
7. 用户也可以临时额外粘贴模板内容，会与文件夹中的模板合并使用

放入约 8-10 篇爆款文案（.txt 或 .md），Skill 触发时自动读取全部文件作为风格参考。

---

## 视频辅助工具（低字幕时）

当用户同时提供视频和带时间轴的字幕，且字幕信息稀疏时，使用本 Skill 自带脚本抽取关键画面，辅助理解字幕没有写出的动作和场面。

脚本位置：`scripts/video_context.py`

命令格式：
```
python3 scripts/video_context.py --subtitle path/to/episode.txt --video path/to/episode.mp4
```

可选参数：
- `--max-points 18`：最多抽取多少个画面点，默认 18
- `--gap-threshold 3.0`：超过多少秒无字幕视为长空白，默认 3 秒
- `--out path/to/output`：指定输出目录

默认行为：
- 如果未显式提供 `--video`，脚本会尝试在字幕同目录查找同名视频
- 优先使用系统已安装的 `ffmpeg` / `ffprobe`
- 如果没有安装，macOS / Windows 首次运行会自动下载到用户缓存目录，不修改系统 PATH
- Linux 暂不自动下载，提示用户自行安装 `ffmpeg` / `ffprobe`
- 如果字幕没有时间轴，不要猜时间点；提示用户提供带时间轴字幕，或提供关键画面描述

输出产物：
- `manifest.json`：记录视频、字幕、低字幕判断、抽帧时间点、图片路径
- `sheets/sheet_001.jpg`：带时间戳的拼图，优先给 AI 分析
- `frames/frame_001_00-01-12.jpg`：单帧图，只有拼图看不清时再查看

⚠️ 抽帧结果只用于内部理解。不要把 `manifest.json`、画面事实清单、图片路径输出给用户，除非用户明确要求排查抽帧结果。

---

## 工作流程

整个流程分为两个阶段：**深度拆解与中文文案生成** 和 **英文翻译**。先完成中文文案，等用户确认满意后，再进行英文翻译。

---

### 阶段一：深度拆解与中文文案生成

#### Step 0 — 读取剧情简介（可选步骤）

这一步是为了获取故事背景和人物关系，**不要输出给用户**。

- 优先检查用户对话中是否提供了以 `剧情简介：` 开头的文本，如果有：读取该段文本作为剧情简介上下文，再进入 Step 1
- 如果对话中未提供：检查用户提供的文件中是否有剧情简介文件
  - 匹配规则：文件名包含"剧情""简介""summary""plot"关键词（不区分大小写）
  - 如果找到：读取剧情简介文件作为上下文，再进入 Step 1
- 如果对话与文件都未提供剧情简介：先向用户确认
  - 提示语：`检测到你没有提供剧情简介。请确认是否不提供？（回复"不提供"继续；或按"剧情简介："格式粘贴简介）`
  - 若用户补充了 `剧情简介：`：读取补充内容后继续
  - 若用户确认不提供：继续进入 Step 1
- 剧情简介内容仅作为内部理解上下文，不出现在最终文案中

#### Step 1 — 深度结构拆解（内部步骤，不对外输出）

这一步从**爆款短剧爽文结构**的角度深度拆解文本，**不要输出给用户**。产出拆解公式供 Step 3 写文案时严格参照。

- 完整读取字幕文件全文
- 如果字幕带时间轴且用户提供了同名视频或视频路径：先判断是否需要进入「低字幕视频辅助」
  - 字幕覆盖率低、单位分钟字幕字数少、或存在多个长无字幕 gap 时，运行 `scripts/video_context.py`
  - 如果脚本输出 `frame_triggered=false`，说明字幕信息足够，继续按字幕分析
  - 如果脚本输出 `frame_triggered=true`，优先查看 `sheets/` 中的拼图；只有拼图看不清时才查看 `frames/` 中的单帧
  - 将画面分析结果整理成内部「画面事实清单」：时间点 + 客观画面事实
  - 画面事实清单只能作为内部素材，不输出给用户
- 如果无法分析图片（当前模型或环境不支持视觉输入），提示用户提供关键画面描述；用户不补充时，只能按字幕保守生成
- 梳理故事线：起因 → 冲突升级 → 转折点 → 高潮 → 结局
- **正常使用人物名**，如实概括剧情
- 对画面里不确定的身份、关系、动作，不要写成确定事实；用保守表达概括

然后从以下 **10 个维度** 进行深度拆解：

##### 1. 开场钩子
- 开头是否反常？是否制造疑问？
- 是否用了"异常行为 / 被羞辱 / 身份错位 / 偷情 / 误会"等高钩子事件？
- 观众在前 3 秒会不会被抓住？

##### 2. 主角情绪位置
- 主角一开始处于什么低位？
- 被谁压制？被夺走了什么？
  （身份、爱情、资源、功劳、地位、家庭偏爱等）
- 低位越具体，后续翻身越爽

##### 3. 反派升级路径
- 反派如何一步步"连续作死"？
  **羞辱 → 误会 → 抢夺 → 栽赃 → 公开羞辱 → 否认身份 → 逼入绝境**
- 每一步是否比前一步更恶劣？

##### 4. 信息差设计
- 观众知道什么？
- 反派不知道什么？
- 文中角色不知道什么？
- 文案如何利用"观众比角色更早知道真相"制造爽感？
- 信息差越明显，观众代入感越强

##### 5. 延迟打脸
- 主角为什么不立刻解释？
- 是否故意隐忍？是否等待公开场合？
- 是否利用反派继续作死来放大最终爽点？
- 延迟越长，打脸越爽

##### 6. 节奏与反转
- 每隔几句是否就有一个小冲突或小反转？
- 使用了哪些节奏词？
  "下一秒"、"没想到"、"就在这时"、"原来"、"关键时刻"、"然而"、"却"等
- 文案如何避免观众停顿走神？

##### 7. 爽点类型
判断属于以下哪些爽点（可多选）：
- **身份爽** — 真实身份曝光震撼全场
- **打脸爽** — 反派被当场揭穿/碾压
- **偏爱爽** — 男主/权威人物公开维护主角
- **复仇爽** — 主角亲手反击前仇旧恨
- **反差爽** — 被看低的人突然展现惊人能力
- **权力碾压爽** — 更高权力者直接压制反派
- **豪门爽** — 财富/家族地位碾压
- **重生爽** — 带着前世记忆重新开局的降维打击
- **马甲爽** — 隐藏多重身份层层曝光

##### 8. 男主/权威角色作用
- 是否负责护妻？是否负责公开认可主角？
- 是否是推动身份曝光的杠杆？
- 是否在关键时刻提供资源或背书？

##### 9. 公开处刑场景
- 高潮是否发生在以下公共空间？
  宴会、会议、订婚宴、直播间、家族大厅、公司现场
- **为什么公开场景会增强爽感？**
  — 反派当众丢脸无法抵赖
  — 主角被公开认可形成社会性确认
  — 围观群众的反应放大情绪冲击

##### 10. 卡点设计
- 结尾如何"故意不讲完"？
- 是否留下更大身份？是否留下更大危机？
- 是否停在观众最想继续看的地方？

##### 产出拆解公式（供 Step 3 严格参照）

基于以上 10 个维度的分析，整理出以下四份内部素材：

**A. 核心爆点公式**（用箭头结构表示）
例：信息差建立 → 反派连续作死 → 主角隐忍蓄势 → 公开处刑 → 身份曝光 → 情绪释放

**B. 情绪推进链**
观众情绪是如何一步步被拉高的：
- 第 1 层：好奇/疑问（钩子制造）
- 第 2 层：愤怒/憋屈（主角被压制）
- 第 3 层：期待/着急（信息差形成）
- 第 4 层：爽/解气（打脸或反转）
- 第 5 层：更高级的期待（新钩子/卡点）

**C. 可复用模板**
提炼出一个万能爽文结构，可在其他短剧中直接套用

**D. TikTok/Reels 改编要点**
- 前 3 秒钩子应该怎么写
- 哪些地方必须加重语气
- 哪些地方必须快速剪辑
- 哪些地方适合停顿制造期待

#### Step 2 — 分析爆款文案模板

- **按前面「模板文件夹」章节的步骤**，定位并读取 `templates/` 中的所有模板文件（.txt / .md）
- 如果用户对话中额外提供了模板内容，一并纳入分析
- 如果模板文件夹为空且用户未额外提供，提醒用户放入模板后再继续
- 通读所有模板后，提炼共性特征，包括但不限于：
  - **开头钩子**：用什么方式抓人？（悬念提问 / 冲突预告 / 反转让步 / 情绪共鸣）
  - **叙事节奏**：短句还是长句？几句话一个转折？
  - **情绪基调**：爽文型 / 虐心型 / 悬疑型 / 甜宠型 / 复仇型
  - **人称使用**：第一人称「我」还是第三人称「他/她」？
  - **结尾套路**：总结升华 / 悬念引导 / 互动提问 / 金句收尾
- 总结为 3-5 条的「文案公式」，在生成脚本时严格参照

#### Step 3 — 生成中文文案脚本（核心产物）

**素材来源**：
- Step 1 的深度结构拆解成果（A/B/C/D 公式）
- 字幕原文中的高光台词 / 名场面细节
- 低字幕视频辅助得到的内部画面事实清单（如果有）
- 爆款文案模板的风格公式（Step 2）

**字数**：800-1000 字

**硬性约束**：
- ⚠️ **绝对不允许出现任何人物名字**。全部替换为：他、她、男人、女人、男主、女主、那个人、对方 等代词
- 如果字幕中人物名反复出现，必须全部改写
- 保留剧中关键冲突和名场面细节（用代词指代即可）
- ⚠️ **禁止使用对话/引号** — 不能出现带双引号的直接对话，所有内容用第三人称转述
- ⚠️ **必须输出为一整段** — 全文连续书写，不分段、不换行、不另起一行，句号后不留空行

**结构要求**（在一整段内完成）：
- **钩子开头**：严格参照 Step 1 拆解的"开场钩子分析"，用悬念/冲突/反差抓住观众
- **正文展开**：按照 Step 1 拆解的"情绪推进链"推进叙事，保持每 2-3 句一个小节奏
  - 保留反派升级路径中的关键节点
  - 利用信息差设计制造观众优越感
  - 在延迟打脸处做足蓄势
  - 公开处刑场景需重点渲染围观群众反应
- **结尾收束**：利用 Step 1 拆解的"卡点设计"，留下悬念或勾子
  - 如果源剧有后续，用"然而更大的危机还在后面"类收尾
  - 如果是一集完结，用金句收尾并引导互动

**风格要求**：
- 短句为主，适合短视频配音节奏
- 口语化但不口水话，有情绪张力
- 严格模仿 Step 2 分析的爆款模板叙事语气
- 整段文字用第三人称连贯叙述，不出现第一人称
- 适当使用节奏词（"下一秒""没想到""就在这时""原来""关键时刻"）推进节奏

#### Step 3 完成后 — 停下来等用户反馈

**必须使用分隔符格式输出**（供 Electron 桌面应用解析）。只输出中文文案脚本，不要输出剧情概述。输出后**必须停止，不要自动翻译**。

输出格式：
```
<<<DRAMA_SCRIPT>>>
[中文文案脚本文本...]
<<<END_SCRIPT>>>
```

分隔符 `<<<DRAMA_SCRIPT>>>` 和 `<<<END_SCRIPT>>>` 必须各独占一行，中间是纯文本内容。

然后主动询问用户：
> 「中文文案已生成，需要修改什么地方吗？确认没问题后我就翻译成英文。」

然后等待用户反馈。用户可能会要求：
- 调整语气（更激烈 / 更温柔 / 更悬疑）
- 增减字数
- 修改某一段落
- 调整钩子开头或结尾

**特殊判断：用户确认最终版**
如果用户消息包含以下关键词之一，识别为"最终版确认"，**跳过修改迭代，直接进入 Step 4 翻译**：
- "最终版"、"最终版本"
- "就这样"、"可以了"、"没问题"、"确认"
- "翻译吧"、"翻译"

同时，如果用户发来的内容以 `<<<DRAMA_SCRIPT>>>` 开头，同样视为最终版，提取分隔符内的文本作为最终中文文案，直接进入翻译。

不满足以上条件时，走原有的迭代修改流程。

---

### 阶段二：英文翻译（仅在用户确认后执行）

#### Step 4 — 翻译为口语化英文

- 基于用户最终确认的中文文案脚本进行翻译
- **不是逐字直译**，而是口语化改写
- 保持短视频文案的节奏感（短句、有力）
- 适合 TikTok / YouTube Shorts / Instagram Reels 的表达习惯
- 英文中同样不使用人物名，用 he / she / the man / the woman 代替
- 充分利用 Step 1 拆解的"TikTok/Reels改编建议"：
  - 前 3 秒用强钩子英文表达
  - 关键冲突点加重语气词
  - 节奏词处适合快速剪辑切换
  - 重要反转前留停顿空间
- ⚠️ **必须输出为一整段** — 不分段、不换行，连续书写

**必须使用分隔符格式输出**：
```
<<<DRAMA_ENGLISH>>>
[English script text...]
<<<END_ENGLISH>>>
```

分隔符必须各独占一行，中间是纯文本内容。

---

## 输出格式（Electron 应用解析标准）

本 Skill 的输出供 Electron 桌面应用通过 CLI stdout 解析。两个阶段使用不同的分隔符标记。

### 阶段一：中文文案脚本

```
<<<DRAMA_SCRIPT>>>
[800-1000 字中文解说文案，纯文本，不使用人物名，无对话引号，一整段连续书写]
<<<END_SCRIPT>>>
```

### 阶段二：英文翻译

```
<<<DRAMA_ENGLISH>>>
[口语化英文翻译，纯文本，不使用人物名，无对话引号，一整段连续书写]
<<<END_ENGLISH>>>
```

### 分隔符规则

1. 分隔符**独占一行**，前后不要加任何其他文字
2. `<<<DRAMA_SCRIPT>>>` 和 `<<<END_SCRIPT>>>` 成对出现
3. `<<<DRAMA_ENGLISH>>>` 和 `<<<END_ENGLISH>>>` 成对出现
4. 分隔符之间的内容为**纯文本**，不要用 Markdown 格式
5. 绝对不要在分隔符之外的文本中使用 `<<<` 前缀，避免解析错误
6. 分隔符大小写必须完全一致

---

## 注意事项

1. **剧情概述有人物名，文案脚本无人名** — 这是最重要的规则，不要搞混
2. **深度拆解是内部步骤** — Step 1 的 10 维度分析和 A/B/C/D 公式仅作为内部素材，不出现在最终文案中，除非用户明确要求查看拆解过程
3. 如果字幕文件很长（超过 1000 行），先完整读取再做拆解，不要跳读
4. 爆款模板分析要落到实处 — 生成的脚本必须能看得出模板的影子
5. **阶段一完成后不要自动翻译**，必须等用户确认后才进入阶段二
6. 英文翻译是口语化翻译，不是书面翻译 — 读起来要像 native speaker 在讲故事
7. 用户修改中文文案期间，耐心迭代，每次修改后重新用分隔符输出完整中文文案
8. **分隔符必须严格使用**，这是 Electron 桌面应用识别内容的唯一依据
9. **文案整段输出，不能分段分行** — 中英文都是一整段连续文字，句号后不空行、不另起一行，确保 Electron 应用解析为单段文本
10. **禁止直接对话** — 所有台词用第三人称转述，不能用引号包裹的对话形式
11. **剧情简介仅用于内部理解** — 可来自对话 `剧情简介：` 或简介文件，只作为理解角色关系的上下文，不直接出现在文案中
12. **"最终版"即翻译** — 用户说"最终版""确认""翻译吧"或发回带分隔符的内容，视为最终版直接翻译，不再迭代修改
13. **低字幕视频辅助只补事实，不负责脑补剧情** — 画面分析只能写客观可见内容，不能把不确定动作、身份、关系写成确定事实
14. **抽帧产物默认不展示** — `manifest.json`、拼图、单帧路径和内部画面事实清单都不要出现在最终文案或常规回复里
15. **v2.0 核心升级** — 不再只是总结剧情，而是从"爆款短剧爽文结构"视角深度拆解情绪推进、信息差、打脸节奏、爽点类型等维度，生成更具传播力的解说文案
