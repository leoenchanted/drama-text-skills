---
name: drama-text-skills
description: 短剧解说文案生成器 - 从字幕文件生成剧情概述、爆款文案脚本（300-400字）及口语化英文翻译。触发词：短剧文案、解说脚本、字幕生成文案、drama copywriting。
metadata:
  tags: copywriting, short-drama, subtitle, script, translation
---

## 触发场景

当用户说「帮我给这个短剧写解说文案」「把字幕生成文案脚本」「drama copywriting」等，或提供字幕 .txt 文件要求生成短视频解说文案时，使用此 Skill。

## 输入要求

用户需要提供：
1. **短剧字幕文件**（.txt 格式）

## 模板文件夹（重要）

模板文件夹位于**本 SKILL.md 文件所在目录下的 `templates/` 子文件夹**。

查找步骤：
1. 本 SKILL.md 位于 `.claude/skills/drama-text-skills/SKILL.md`
2. 模板文件夹就在同目录：`.claude/skills/drama-text-skills/templates/`
3. 先用 `ls .claude/skills/drama-text-skills/templates/` 确认目录存在
4. 如果报「目录不存在」或「无法访问」，说明 CWD 不是项目根目录。此时用 `find . -path "*/drama-text-skills/templates" -type d` 定位实际路径，然后读取
5. 读取其中所有模板文件（.txt / .md）
6. 如果模板文件夹为空，提醒用户先把爆款文案模板放入
7. 用户也可以临时额外粘贴模板内容，会与文件夹中的模板合并使用

放入约 8-10 篇爆款文案（.txt 或 .md），Skill 触发时自动读取全部文件作为风格参考。

---

## 工作流程

整个流程分为两个阶段：**中文文案生成** 和 **英文翻译**。先完成中文文案，等用户确认满意后，再进行英文翻译。

---

### 阶段一：中文文案生成

#### Step 1 — 生成剧情概述（内部步骤，不对外输出）

这一步是为了让你自己理解剧情，**不要输出给用户**。

- 完整读取字幕文件全文
- 梳理故事线：起因 → 冲突升级 → 转折点 → 高潮 → 结局
- **正常使用人物名**，如实概括剧情
- 这份概述作为 Step 3 写文案时的素材参考

#### Step 2 — 分析爆款文案模板

- **按上面「模板文件夹」章节的步骤**，定位并读取 `templates/` 中的所有模板文件（.txt / .md）
- 如果用户对话中额外提供了模板内容，一并纳入分析
- 如果模板文件夹为空且用户未额外提供，提醒用户放入模板后再继续
- 通读所有模板后，提炼共性特征，包括但不限于：
  - **开头钩子**：用什么方式抓人？（悬念提问 / 冲突预告 / 反转让步 / 情绪共鸣）
  - **叙事节奏**：短句还是长句？几句话一个转折？
  - **情绪基调**：爽文型 / 虐心型 / 悬疑型 / 甜宠型 / 复仇型
  - **人称使用**：第一人称「我」还是第三人称「他/她」？
  - **结尾套路**：总结升华 / 悬念引导 / 互动提问 / 金句收尾
- 总结为 3-5 条的「文案公式」，在生成脚本时严格参照

#### Step 3 — 生成中文文案脚本（核心产物）

**素材来源**：
- 剧情概述（Step 1）
- 字幕原文中的高光台词 / 名场面细节
- 爆款文案模板的风格公式（Step 2）

**字数**：300-400 字

**硬性约束**：
- ⚠️ **绝对不允许出现任何人物名字**。全部替换为：他、她、男人、女人、男主、女主、那个人、对方 等代词
- 如果字幕中人物名反复出现，必须全部改写
- 保留剧中关键冲突和名场面细节（用代词指代即可）
- ⚠️ **禁止使用对话/引号** — 不能出现带双引号的直接对话，所有内容用第三人称转述
- ⚠️ **必须输出为一整段** — 全文连续书写，不分段、不换行、不另起一行，句号后不留空行

**结构要求**（在一整段内完成）：
- **钩子开头**（前 50 字）：用悬念/冲突/反差抓住观众，让他们停下来看
- **正文展开**（200-300 字）：讲清楚发生了什么，保留关键冲突和情绪高潮
- **结尾收束**（最后 50 字）：情绪升华、悬念引导、或一句话点评

**风格要求**：
- 短句为主，适合短视频配音节奏
- 口语化但不口水话，有情绪张力
- 模仿爆款模板的叙事语气
- 整段文字用第三人称连贯叙述，不出现第一人称

#### Step 3 完成后 — 停下来等用户反馈

**必须使用分隔符格式输出**（供 Electron 桌面应用解析）。只输出中文文案脚本，不要输出剧情概述。输出后**必须停止，不要自动翻译**。

输出格式：
```
<<<DRAMA_SCRIPT>>>
[中文文案脚本文本...]
<<<END_SCRIPT>>>
```

分隔符 `<<<DRAMA_SCRIPT>>>` 和 `<<<END_SCRIPT>>>` 必须各独占一行，中间是纯文本内容。

然后主动询问用户：
> 「中文文案已生成，需要修改什么地方吗？确认没问题后我就翻译成英文。」

然后等待用户反馈。用户可能会要求：
- 调整语气（更激烈 / 更温柔 / 更悬疑）
- 增减字数
- 修改某一段落
- 调整钩子开头或结尾

根据用户反馈反复修改，每次修改后**重新用分隔符格式输出完整中文文案**，直到用户明确表示「确认」「可以」「没问题」「翻译吧」等信号。

---

### 阶段二：英文翻译（仅在用户确认后执行）

#### Step 4 — 翻译为口语化英文

- 基于用户最终确认的中文文案脚本进行翻译
- **不是逐字直译**，而是口语化改写
- 保持短视频文案的节奏感（短句、有力）
- 适合 TikTok / YouTube Shorts / Instagram Reels 的表达习惯
- 英文中同样不使用人物名，用 he / she / the man / the woman 代替
- ⚠️ **必须输出为一整段** — 不分段、不换行，连续书写

**必须使用分隔符格式输出**：
```
<<<DRAMA_ENGLISH>>>
[English script text...]
<<<END_ENGLISH>>>
```

分隔符必须各独占一行，中间是纯文本内容。

---

## 输出格式（Electron 应用解析标准）

本 Skill 的输出供 Electron 桌面应用通过 CLI stdout 解析。两个阶段使用不同的分隔符标记。

### 阶段一：中文文案脚本

```
<<<DRAMA_SCRIPT>>>
[300-400 字中文解说文案，纯文本，不使用人物名，无对话引号，一整段连续书写]
<<<END_SCRIPT>>>
```

### 阶段二：英文翻译

```
<<<DRAMA_ENGLISH>>>
[口语化英文翻译，纯文本，不使用人物名，无对话引号，一整段连续书写]
<<<END_ENGLISH>>>
```

### 分隔符规则

1. 分隔符**独占一行**，前后不要加任何其他文字
2. `<<<DRAMA_SCRIPT>>>` 和 `<<<END_SCRIPT>>>` 成对出现
3. `<<<DRAMA_ENGLISH>>>` 和 `<<<END_ENGLISH>>>` 成对出现
4. 分隔符之间的内容为**纯文本**，不要用 Markdown 格式
5. 绝对不要在分隔符之外的文本中使用 `<<<` 前缀，避免解析错误
6. 分隔符大小写必须完全一致

---

## 注意事项

1. **剧情概述有人物名，文案脚本无人名** — 这是最重要的规则，不要搞混
2. 如果字幕文件很长（超过 1000 行），先完整读取再做概述，不要跳读
3. 爆款模板分析要落到实处 — 生成的脚本必须能看得出模板的影子
4. **阶段一完成后不要自动翻译**，必须等用户确认后才进入阶段二
5. 英文翻译是口语化翻译，不是书面翻译 — 读起来要像 native speaker 在讲故事
6. 用户修改中文文案期间，耐心迭代，每次修改后重新用分隔符输出完整中文文案
7. **分隔符必须严格使用**，这是 Electron 桌面应用识别内容的唯一依据
8. **文案整段输出，不能分段分行** — 中英文都是一整段连续文字，句号后不空行、不另起一行，确保 Electron 应用解析为单段文本
9. **禁止直接对话** — 所有台词用第三人称转述，不能用引号包裹的对话形式
